# `tempt`

```
usage: tempt ARG [ARG ...]

Run ARG in a temporary directory and then cleanup.

  -preserve
    	preserve temporary directory
  -version
    	print version and exit

Arguments:
  ARG    Command to run, followed by optional arguments
Environment variables:
  TMPDIR Location in which to create the temporary directory
```
